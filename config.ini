[GLOBAL]
labels = [nothing, something, something else]
instances = 20
channels = 2
frame_length = 3000
algos = [Voting, MLP, SVM (Linear), SVM (Poly), SVM (RBF), Random Forest]
algo_suggestion = 1
curr_algo_index = 3
open_splash = 1

[DS]
ds_handlers = [Camera, Teensy, Arduino, Microphone, Microphone WAV, Mobile UDP, Mobile BLE]
ds_filenames = [ds_camera, ds_teensy, ds_arduino, ds_microphone, ds_microphonewav, ds_mobile_udp, ds_mobile_ble]
ds_file_num = 2
sample_rate = 48000

[ML]
num_bins = 300

[DS_arduino]
t_record = 1
t_overlap = 0.5

