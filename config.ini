[GLOBAL]
open_splash = 0
labels = [no touch, touch, monitor, keyboard]
instances = 10
channels = 1
frame_length = 600
algos = [Voting, MLP, SVM (Linear), SVM (Poly), SVM (RBF), Random Forest]
algo_suggestion = 5
curr_algo_index = 5

[DS]
ds_handlers = [Camera, Teensy, Arduino, Microphone, Microphone WAV, Mobile UDP, Mobile BLE, nano33]
ds_filenames = [ds_camera, ds_teensy, ds_arduino, ds_microphone, ds_microphonewav, ds_mobile_udp, ds_mobile_ble, ds_nano33]
ds_file_num = 3
sample_rate = 48000

[ML]
num_bins = 30

[DS_nano33]
baud_rate = 115200

